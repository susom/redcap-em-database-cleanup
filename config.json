{
  "name": "Database Cleanup",

  "namespace":"Stanford\\DatabaseCleanup",

  "description": "A module for cleaning up the REDCap database",

  "authors": [
    {
      "name": "Andy Martin",
      "email": "andy123@stanford.edu",
      "institution": "Stanford University"
    }
  ],

  "links": {
    "control-center": [
      {
        "name": "Database Cleanup",
        "icon": "gear",
        "url": "menu.php"
      }
    ],
    "project": [
    ]
  },

  "system-settings": [
    {
      "key": "collision-event-gap",
      "name": "<b>Gap in time (seconds) for collision detection</b><br>Defaults to <= 2 seconds",
      "required": false,
      "type": "text"
    },
    {
      "key": "enable-system-debug-logging",
      "name": "<b>Enable Debug Logging (system-wide)</b>",
      "required": false,
      "type": "checkbox"
    }
  ],

  "project-settings": [
  ]
}
